{
  "name": "discord-nordic",
  "version": "1.0.2",
  "description": "Replugged (rewrite) nordic theme",
  "engines": {
    "node": ">=14.0.0"
  },
  "scripts": {
    "build": "replugged build theme",
    "watch": "replugged build theme --watch",
    "bundle": "replugged bundle theme",
    "build-and-bundle": "NO_INSTALL=true pnpm run build && pnpm run bundle",
    "lint": "prettier ./src --check",
    "lint:fix": "prettier ./src --write"
  },
  "keywords": [],
  "author": "ryand",
  "devDependencies": {
    "@parcel/config-default": "^2.8.3",
    "@parcel/core": "^2.8.3",
    "@parcel/transformer-sass": "^2.8.3",
    "@types/node": "^18.11.15",
    "parcel": "^2.8.2",
    "prettier": "^2.8.1",
    "replugged": "4.0.0-rc.2",
    "tsx": "^3.12.1"
  }
}
